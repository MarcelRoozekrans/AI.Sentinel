BenchmarkDotNet.Artifacts/

# Build artifacts
bin/
obj/
*.user

# Local tooling state (Claude Code session, etc.)
.claude/

# Node tooling (scripts/capture-screenshots.mjs uses Playwright)
node_modules/
scripts/package-lock.json
